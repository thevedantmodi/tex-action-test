on: [push]

jobs:
  build-tex:
    runs-on: ubuntu-latest
    name: Build all tex files to PDF
    container: node:16
    steps:
      - run: node --version
      - uses: actions/checkout@v4
      - run: ls
      
